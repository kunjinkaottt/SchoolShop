// 该文件用于创建Vuex中最核心的store
import { createStore } from 'vuex'

export default createStore({
  //用于存储数据
  state: {
  },
  //用于操作数据
  mutations: {
  },
  //用于响应组件中的动作
  actions: {
  },
  //
  modules: {
  }
})
